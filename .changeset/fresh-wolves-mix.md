---
"api": patch
"web": patch
---

Fix formatting in GitHub App installation URL construction to satisfy CI `oxfmt --check`.

---
EOF && git add .changeset/fresh-wolves-mix.md && git commit -m "chore: add patch changeset for format fix"